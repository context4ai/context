# Legacy plugin source path

The maintained source moved to `plugins/context/` at the repository root.
Everything below this legacy directory is a Phase G deletion candidate and
must not regain an independently editable plugin body or manifest template.
