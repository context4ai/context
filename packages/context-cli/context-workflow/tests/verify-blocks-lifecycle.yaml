schema: agent-graph.test.v1
name: blocking verification precedes lifecycle mutations
graph: workspace
entry: context
state:
  facts:
    workspace: { project_entry_valid: true, state_valid: true }
    verification: { blocking_clear: false }
expect:
  statusCode: actionable
  primaryNode: repair-verification
  primaryReasonCode: route.verify.failed
  requiredResources: [procedure.verify-and-repair, context.verification-current]
