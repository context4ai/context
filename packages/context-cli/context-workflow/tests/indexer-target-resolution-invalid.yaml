schema: agent-graph.test.v1
name: an invalid author Result blocks before acceptance
graph: indexer
entry: main-run-validation
state:
  outcomes:
    indexer/validate-main-index-author-result: failed
expect:
  statusCode: blocked
