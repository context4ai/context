schema: agent-graph.test.v1
name: managed structure authority skips report inspection and resolves directly
graph: workspace
entry: context
state:
  authorities: [context.structure-confirmation]
  facts:
    workspace: { project_entry_valid: true, state_valid: true }
    verification: { blocking_clear: true }
    evidence: { maintenance_clear: true }
    close: { current: true }
    gates:
      evidence_maintenance_resolved: true
    resume:
      prose_declarations_complete: true
      structure_confirmation_resolved: false
      structure_confirmed: false
expect:
  statusCode: actionable
  primaryNode: confirm-structure
  primaryReasonCode: route.structure.confirmation-required
  gateResolution: session-authority
  resolutionHandler: context.structure.confirm
  requiredResources: []
