schema: agent-graph.test.v1
name: authorized evidence maintenance resolves to the typed batch action
graph: workspace
entry: context
state:
  facts:
    workspace: { project_entry_valid: true, state_valid: true }
    verification: { blocking_clear: true }
    evidence: { maintenance_clear: false }
    indexer: { lifecycle_current: true, registry_state: current }
    gates: { evidence_maintenance_resolved: true }
expect:
  statusCode: actionable
  primaryNode: maintain-evidence
  primaryReasonCode: route.evidence.maintenance-required
  command: context review maintain --input .tmp/agent-payloads/evidence-maintenance.json --format json
