schema: agent-graph.test.v1
name: document capture declarations are required before source authorization
graph: workspace
entry: context
state:
  facts:
    workspace: { task_start_authorized: true, project_entry_valid: true, state_valid: true }
    verification: { blocking_clear: true }
    indexer: { lifecycle_current: true, registry_state: current }
    close: { current: true }
    gates:
      source_read_resolved: false
      extraction_scope_resolved: true
      document_classification_resolved: true
      structure_confirmation_resolved: true
      knowledge_review_resolved: true
    resume:
      prose_declarations_complete: true
      structure_confirmation_resolved: true
      structure_confirmed: true
      compile_complete: true
      knowledge_review_resolved: true
      review_gate_clear: true
    sources: { registered: true, repositories_ready: true }
    capture: { declarations_complete: false, batch_digest: null, complete: false, route_satisfied: false }
expect:
  statusCode: actionable
  primaryNode: configure-document-capture
  primaryReasonCode: route.capture.configuration-required
  handler: context.project.configure-capture
