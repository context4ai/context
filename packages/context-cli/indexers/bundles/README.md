# Legacy Indexer source path

The maintained community Indexer Skills moved to
`plugins/context/skills/context-*-indexer/` at the repository root. The empty
legacy bundle directory is retained only as a Phase G deletion candidate.
