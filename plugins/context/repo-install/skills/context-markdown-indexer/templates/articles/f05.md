---
id: context.article.f05
kind: procedure
media-type: text/markdown
program:
  article: f05
  policies:
    - standard
  sections:
    clients: 请求层与客户端
    catalog: 接口与调用入口
    mapping: 输入输出与转换
    errors: 错误与重试
    boundaries: 契约与外部边界
    references: 源码入口
---

# F05 API 集成

This is a writing blueprint, not a completion gate. Adapt the headings and level of detail to the reader, evidence and language. Missing or additional chapters and ordinary version differences are advisory. Preserve real source coordinates and a useful next investigation step.

生成条件：已定位请求封装、契约定义或实际调用位置中的至少一类。只有定义时可提供契约／检索入口，但不得把它描述为已确认的页面调用；调用目录只列已证实关系。

建议章节：**请求架构 → 按能力分组的调用目录 → 参数与上下文注入 → 响应、错误和重试 → 契约来源与生成方式 → 服务端阅读入口**。

目录列：页面／能力、调用符号、method/path 或 RPC identity、调用位置、契约来源、已知后端入口。客户端定义与实际调用分列，远程 SDK 内部未知调用不得列为已证实接口。跨仓链只延伸到已登记且有证据的边界。

## 图示选择

请求章节按已证实调用用 sequenceDiagram；参数和 API 坐标保留表格，契约定义不等于调用发生。 图放在对应解释章节，图前说明目的与范围，图后给来源和继续位置；按已下发的共用图示指导编写，无增益时省略，不增加独立必填章节。
