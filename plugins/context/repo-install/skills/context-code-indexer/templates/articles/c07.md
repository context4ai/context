---
id: context.article.c07
kind: procedure
media-type: text/markdown
program:
  article: c07
  policies:
    - compact
    - standard
    - expanded
  sections:
    entries: 问题到入口
    identity: 源码身份与版本
    reading: 推荐阅读顺序
    boundaries: 外部边界
    evidence: 进一步证据
---

# C07 从知识继续查源码

This is a writing blueprint, not a completion gate. Adapt the headings and level of detail to the reader, evidence and language. Missing or additional chapters and ordinary version differences are advisory. Preserve real source coordinates and a useful next investigation step.

适用：入口优先的业务知识包，尤其是只持有 dist 的消费 Agent。建议章节为 **问题到入口的映射 → 源码身份与版本 → 推荐阅读顺序 → 外部边界 → 何时需要进一步证据**。

定位记录：仓库身份、仓内路径／符号、证据版本、已证明到哪一层、接着读什么。在包内返回可执行的阅读指引；工作区 inspect 与包外源码归因遵循各自现有能力和授权，不声称普通 dist query 会自动 clone。无需在每篇文章重复整套工作流，只保留本篇起点和相关指引链接。

## 图示选择

源码坐标优先表格，阅读顺序可用 text 树；跨边界路径有证据再画 flowchart，并写下一步。 图放在对应解释章节，图前说明目的与范围，图后给来源和继续位置；按已下发的共用图示指导编写，无增益时省略，不增加独立必填章节。

## 从现象进入代码，而不是罗列源码

优先给 **问题／现象 → 第一入口 → 下一份证据 → 分支条件**。例如：

| 现象 | 第一入口 | 下一步证据与分支 |
| --- | --- | --- |
| 页面不可见 | 已确认的路由／菜单注册位置 | 检查访问控制与功能配置；先确认页面是否被挂载 |
| 提交无结果 | 页面事件处理与请求调用点 | 请求未发出则查前端条件；已发出再核对响应和服务端入口 |
| 生成类型不一致 | 实际生成配置和消费位置 | 比对输入版本与生成差异；不能用声明证明页面实际调用 |

表中的具体文件应来自本篇证据，示例不替代取证。已证实调用链要求逐跳证据；调查路线
允许某一跳未知，但必须标明“待查”以及如何定位，不能画成已经发生的调用。
跨模块 composer 因证据不足不产出完整链路时，本篇仍保留已知入口和明确的证据缺口。

给出仓库、版本和精确搜索范围。确有相应工具时可提供命令，例如
`rg -n --fixed-strings 'SubmitOrder' apps/orders/src`，说明如何根据命中进入实现／调用方，
以及无命中时该查别名、生成客户端还是外部依赖。使用真实符号替换例子，避免无范围全仓搜索。

包内文章用可解析链接，交付后检查实际目标；源码位置使用 repo@revision:path#symbol，
不要把工作区 knowledge 路径或反引号文本当成 dist 中可跳转的链接。文件名改变后的导航
遵循现有打包映射，不手改 dist 或虚构尚未交付的文章。
