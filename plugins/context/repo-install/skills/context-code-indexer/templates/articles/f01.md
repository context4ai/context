---
id: context.article.f01
kind: procedure
media-type: text/markdown
program:
  article: f01
  policies:
    - compact
    - standard
    - expanded
  sections:
    architecture: 架构总览
    stack: 技术栈与运行方式
    startup: 启动与分层
    crosscutting: 关键横切能力
    deployment: 部署边界
    references: 相关入口
---

# F01 架构

This is a writing blueprint, not a completion gate. Adapt the headings and level of detail to the reader, evidence and language. Missing or additional chapters and ordinary version differences are advisory. Preserve real source coordinates and a useful next investigation step.

生成条件：已定位启动入口与关键运行边界。

建议章节：**架构总览 → 技术栈与运行方式 → 启动与分层 → 关键横切能力 → 部署边界 → 相关入口**。

内容：启动／挂载文件、宿主与子应用职责、路由／状态／请求层的连接、跨层引用。架构图的每条边须有配置、调用或文档依据；历史设计原因需决策材料，不能从当前实现推断。远程模块身份不等于宿主身份。

## 图示选择

架构章节用 flowchart LR，按启动、宿主和请求层分组，每条边有来源。 图放在对应解释章节，图前说明目的与范围，图后给来源和继续位置；按已下发的共用图示指导编写，无增益时省略，不增加独立必填章节。

## 多模块工程地图写法

以已确认的模块为行，给出 **仓内路径 → 职责 → 首要注册／启动入口 → 深入位置**。
一级工程先区分宿主、应用、共享库和工具，再按需要展开业务子模块；不把每个子目录
自动认作一个应用。相同框架的模块合并说明，只展开有实际差异的运行边界。

匿名示例（路径只示范组织方式，不是默认框架约定，产出时替换为实际证据）：

| 模块 | 职责 | 首要入口 | 下一步 |
| --- | --- | --- | --- |
| apps/portal | 装载页面与公共导航 | src/bootstrap.ts | 路由注册、访问控制和模块装载位置 |
| apps/portal/modules/orders | 订单查询与详情 | src/routes.ts | 对应页面的数据请求与状态分支 |
| packages/widgets | 跨页面组件 | src/index.ts | 组件消费说明与实际调用方 |

将路由、权限、请求、状态、宿主桥接等横切入口集中说明，业务模块页回链并写自身差异。
结构图用于解释关系，定位表用于找文件，两者无需互相复述。目录中无法证实的职责标为待查，
不按目录名编造业务能力。大范围材料只提供地图时，说明样本与未读范围。
